SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_smtp_password_or_api_key
SMTP_FROM=your_email@example.com
SMTP_USE_SSL=false
SMTP_USE_TLS=true

# For local development, OTPs are also appended to otp_dev.log
# when SMTP settings are missing. In production, ensure SMTP_* are set.
